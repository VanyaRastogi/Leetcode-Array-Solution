class Solution {
    public boolean search(int[] nums, int target) {
        int s=0 , e=nums.length-1;
        while(s<=e){
            if(nums[s]!=target)s++;
            else return true;
            if(nums[e]!=target)e--;
            else return true;
            
        }return false;
    }
}
